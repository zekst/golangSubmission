const Home = () => {
    return (
        <div>
            {'You are not logged in. Kindly register yourself. If already registered proceed to login. Thanks!!'}
        </div>
    );
};

export default Home;
